import React from "react";
import { AppBar } from "@material-ui/core";

const Header = () => (
  <AppBar position="relative">
    <h1 id="app-title">Shopping Comparison Tool</h1>
  </AppBar>
);

export default Header;
